# nixpacks.toml
# Configuration pour le build Nixpacks sur Railway.
# Ce fichier indique à l'environnement de déploiement quelles extensions PHP installer.

[phases.setup]
nixPkgs = [
    "php",
    "phpExtensions.pdo",
    "phpExtensions.pdo_mysql",
    "phpExtensions.curl"
]